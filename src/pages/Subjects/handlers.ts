/**
 * Handles subject selection. Navigates to the subject details page or logs the subject.
 * @param subject The selected subject object.
 * @throws No exceptions are thrown by this function.
 */
export function handleSelectSubject(subject: import("../../types/subjects/Subject").Subject) {
    console.log('Selected subject:', subject);
}

export interface CommandConfig {
    mode: string;
    commandCount: number;
    studentsCount: number;
    segregationType: string;
    enableCommander: boolean;
    minBound: number;
    maxBound: number;
}
